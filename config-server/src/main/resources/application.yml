spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        default-label: main
        git:
          uri: https://github.com/altananay/config-server
  profiles:
    active: dev
server:
  port: 8888